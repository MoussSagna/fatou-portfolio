import { SectionShell } from '@/components/layout/SectionShell'

/** Content arrives in its dedicated sprint (see docs/sprints). */
export function ProjectsSection() {
  return <SectionShell id="projets" eyebrow="Projets sélectionnés" className="min-h-[60vh]" />
}
