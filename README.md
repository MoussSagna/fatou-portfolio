# Sarah — Portfolio UI/UX Designer

Portfolio one-page en React + Vite + TypeScript + Tailwind CSS + shadcn/ui + Motion.

```bash
npm install
npm run dev
```

Toute la documentation (architecture, design system, sprints, comparaisons avec la maquette) est
dans [`docs/`](docs/README.md).
